import math
# in the below line we are using the functions inside the math module we just imported
print(math.ceil(2.9)) # celing of the no.
print(math.floor(2.9)) # floor of the no.
x = -2.9
print(round(x)) # rounds the number to its nearest integer value
print(abs(x)) # returns the positve number
